server {
    listen 8080;
    server_name localhost;
    root /var/www/daemon-space;
    index index.html;

    location / {
        try_files $uri $uri/ =404; # Corrected line
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }

    # Additional configurations...
}

/////////////////////////////////////////////////////
